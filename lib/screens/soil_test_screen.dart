import 'package:flutter/material.dart';
class SoilTestScreen extends StatelessWidget {
  const SoilTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
